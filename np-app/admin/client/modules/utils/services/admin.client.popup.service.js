'use strict';

var angular = require('angular');

module.exports = angular.module('utils')
    .factory('AdminPopupService', AdminPopupService);

function AdminPopupService () {
    'use strict';

    var adminPopupService = {};

    

    return adminPopupService;
}
